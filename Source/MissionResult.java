enum MissionResult {
    //this enum will be returned by the Mission class to tell other classes how to react to the user's interactions with a mission
    CLOSE_GAME,
    REMOVE_MARKER,
    KEEP_MARKER
}